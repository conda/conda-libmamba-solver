### Enhancements

* <news item>

### Bug fixes

* <news item>

### Deprecations

* <news item>

### Docs

* <news item>

### Other

* Replace Tsinghua Anaconda mirror channel test with `test_foreign_index_json_does_not_leak_channel`, using a poisoned `.conda` package in the shared `mamba_repo` fixture whose `index.json` embeds foreign channel metadata. (#1027)
