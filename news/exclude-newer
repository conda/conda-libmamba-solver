### Enhancements

* Use libmambapy 2.9.0's native `exclude_newer_timestamp` support for global
  `exclude_newer` policies, with Python filtering for older libmambapy versions
  and scoped policies. (#905, conda/conda#15759)
